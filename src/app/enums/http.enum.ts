export enum Observe {
  BODY = 'body',
  EVENTS = 'events',
  RESPONSE = 'response',
}

/**
 * ResponseType {ARRAYBUFFER, BLOB, JSON, TEXT }
 */
export enum ResponseType {
  ARRAYBUFFER = 'arraybuffer',
  BLOB = 'blob',
  JSON = 'json',
  TEXT = 'text',
}
